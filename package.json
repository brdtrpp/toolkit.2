{
  "name": "toolkit2",
  "private": true,
  "scripts": {
    "start": "meteor reset && meteor run --settings dev-settings.json",
    "deploy": "DEPLOY_HOSTNAME=galaxy.meteor.com meteor deploy --settings production-settings.json toolkit.meteorapp.com",
    "pro": "meteor reset && meteor run --raw-logs --port $IP:$PORT --settings dev-settings.json --production",
    "test": "spacejam test-packages --driver-package=practicalmeteor:mocha-console-runner myaccount:mypkg1 myaccount:mypkg2"
  },
  "dependencies": {
    "@babel/runtime": "^7.0.0-beta.51",
    "babel-runtime": "^6.26.0",
    "bcrypt": "^1.0.3",
    "core-js": "^2.5.7",
    "generate-docx": "^1.3.0",
    "meteor-node-stubs": "^0.2.11",
    "moment": "^2.22.2",
    "npm-check-updates": "^2.14.2",
    "popper.js": "^1.14.3",
    "simpl-schema": "^0.3.2",
    "stripe": "^6.13.0"
  },
  "devDependencies": {
    "phantomjs-prebuilt": "^2.1.16"
  }
}
