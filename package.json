{
  "name": "toolkit2",
  "private": true,
  "scripts": {
    "start": "meteor run",
    "deploy": "DEPLOY_HOSTNAME=us-east-1.galaxy-deploy.meteor.com meteor deploy --settings production-settings.json toolkit.meteorapp.com"
  },
  "dependencies": {
    "babel-runtime": "^6.20.0",
    "bcrypt": "^1.0.2",
    "meteor-node-stubs": "~0.2.4",
    "moment": "^2.18.1",
    "simpl-schema": "^0.3.1"
  }
}
